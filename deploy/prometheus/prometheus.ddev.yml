global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: gateframe-gateway
    metrics_path: /metrics
    static_configs:
      # ddev compose service name (see .ddev/docker-compose.backend.yaml)
      - targets: ["gateway:3000"]
        labels:
          environment: ddev
